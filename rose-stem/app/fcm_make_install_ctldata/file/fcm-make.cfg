steps = $steplist

extract.ns = ctldata stash
extract.location[ctldata] = $HOST_SOURCE_UM_AUX_BASE/ctldata$SOURCE_UM_AUX_REV
extract.path-excl[ctldata] = $UM_AUX_EXCLUDE_PATH

extract.location[stash] = $HOST_SOURCE_UM_BASE$SOURCE_UM_REV
extract.path-excl[stash] = /
extract.path-incl[stash] = rose-meta/um-atmos/$INSTALL_META_VERSION/etc/stash/STASHmaster \
                         \ rose-meta/um-atmos/$INSTALL_META_VERSION/etc/STASH2CF

mirror.target = ${ROSE_TASK_MIRROR_TARGET}
